import React from "react";
import {
  SafeAreaView,
  View,
  Button,
  StyleSheet,
  Text,
  FlatList,
  // StatusBar,
} from "react-native";

// Load the item component
import EpisodeItem from "./EpisodeItem";
// Load episodes
import episodes from "../public/episodes";

const EpisodeList = () => {
    return(
        <FlatList>

        </FlatList>
    )
};

export default EpisodeList;